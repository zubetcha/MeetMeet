import type { NextPage } from "next";
import { DarkModeToggle } from "../components";
const Home: NextPage = () => {
  return (
    <div>
      <DarkModeToggle />
    </div>
  );
};

export default Home;
